import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "images.unsplash.com",
        port: "",
        pathname: "/**",
      },
    ],
  },
  // Allow local network IP for development server
  allowedDevOrigins: ["192.168.1.7"],
};

export default nextConfig;
